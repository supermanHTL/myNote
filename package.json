{
  "name": "my-vuepress",
  "version": "1.0.0",
  "description": "test vuepress ",
  "main": "index.js",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "gh-pages -d docs/.vuepress/dist",
    "deploy:build": "npm run build && gh-pages -d docs/.vuepress/dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "htl",
  "license": "ISC",
  "dependencies": {
    "vuepress": "^1.8.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
